name: first workflow
on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"

  build:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - run: echo "build"